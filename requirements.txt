# Core data / ML
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Gradient boosting
xgboost>=2.0.0
lightgbm>=4.0.0

# NLP
nltk>=3.8.1

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
wordcloud>=1.9.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# Persistence / config
joblib>=1.3.0
pyyaml>=6.0

# Testing
pytest>=7.4.0
httpx>=0.25.0

# ---------------------------------------------------------------------------
# Optional / future-scope (documented in README, not required to run Phase 1-9):
# torch, transformers, sentence-transformers  -> deep learning models (LSTM/BERT)
# gensim                                       -> Word2Vec / FastText
# shap, lime                                    -> model interpretability
# mlflow, dvc                                   -> experiment/data versioning
# optuna                                        -> hyperparameter search
# ---------------------------------------------------------------------------
